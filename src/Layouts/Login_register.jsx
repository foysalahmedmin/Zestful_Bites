import { Outlet } from "react-router-dom";

const Login_register = () => {
    return (
        <>
            <main>
                <Outlet />
            </main>
        </>
    );
};

export default Login_register;